import { useState, useEffect } from "react";

// 頁面偵測到底部的時候就會呼叫這隻Hook
const useInfiniteScroll = (callback) => {
    const [isFetch, setIsFetch] = useState(false);

    useEffect(() => {
        const handleScroll = () => {
            if () {
                
            }
        }
    })


    return isFetch;
}